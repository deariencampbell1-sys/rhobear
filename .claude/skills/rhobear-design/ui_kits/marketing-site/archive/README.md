# Archived pages — kept, not published

Parked 2026-07-10 on the owner's word: the site no longer markets our own runtime/goal
system — that territory belongs to the OpenClaw-native setup, and these pages come back
only if we ever dust it off. Also parked: the beta/demo-era funnels (`spin-it-up`,
`try`) — replaced by real free trials on the live products — and concept pages that
duplicated the product pages (`hub.html` vs `app.html`, `worlds`, `drop-in`, `compass`,
`built-with`).

The Pages deploy only publishes `marketing-site/*.html` (root), so nothing in this
folder ships. To resurrect a page: `git mv` it back to the root and re-add nav links.
